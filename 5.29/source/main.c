#include <stdio.h>
#include <stdlib.h>

int main(void)
{
	int a, b,c;
	printf("叫块ㄢ计 : ");
	scanf("%d%d", &a, &b);
	for (c = 1; c = c; c++){
		if (c%a == 0 & c%b == 0){
			printf("ㄢ计程そ计 : %d\n", c);
			system("pause");
			return 0;
		}
	}
}